{
  "name": "claude-thing-daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code hook install/uninstall — the daemon itself moved to app/src-tauri (Rust)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install-hooks": "node scripts/install-hooks.js",
    "uninstall-hooks": "node scripts/uninstall-hooks.js"
  }
}